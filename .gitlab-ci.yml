image: gcc

build:
  stage: build
  script:
    - apt update && apt -y install make cmake
    - cmake . -DBUILD_TESTS=ON
    - make
  artifacts:
    paths:
      - ./bin/markus-test

test:
  stage: test
  script:
    - ./bin/markus-test